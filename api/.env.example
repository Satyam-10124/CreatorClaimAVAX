# API service (Railway)
# Random secret used to protect mutating endpoints
API_KEY=replace-with-strong-random-string

# EOA private key used by the API to sign on-chain txs on Fuji
# Format: 0x + 64 hex chars
API_PRIVATE_KEY=0xreplace_with_fuji_private_key

# Optional: Platform will inject PORT
# PORT=3000
