{
  "builds": [
    {
      "src": "frontend/package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": "dist"
      }
    }
  ],
  "routes": [
    { "src": "/(assets/.*|favicon.ico|robots.txt)", "dest": "/$1" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
