{
  "name": "creator-claim",
  "version": "1.0.0",
  "description": "CreatorClaim - AI Training Rights & Attribution Protocol",
  "main": "index.js",
  "scripts": {
    "test": "npx hardhat test",
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.js",
    "build": "npx hardhat compile",
    "prestart": "npm run build",
    "start": "npm --prefix api start",
    "postinstall": "npm --prefix api ci && npm --prefix frontend ci || true"
  },
  "keywords": [
    "blockchain",
    "creator",
    "rights",
    "ai",
    "camp"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.0",
    "@openzeppelin/contracts": "^5.4.0",
    "hardhat": "^2.26.1"
  },
  "dependencies": {
    "@chainlink/contracts": "0.6.1",
    "dotenv": "^17.2.1",
    "node-fetch": "^3.3.2"
  }
}
