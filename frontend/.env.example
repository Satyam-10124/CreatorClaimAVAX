# Frontend (Vercel)
# Backend base URL (Railway public URL) without trailing slash
VITE_API_BASE=https://<your-railway-app>.up.railway.app/api

# LOCAL DEV ONLY: API key for testing protected endpoints from the client
# Do NOT set this on Vercel production envs; use server routes if needed
VITE_API_KEY=
